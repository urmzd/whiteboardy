# go.mod is the version pin; the toolchain and the wails CLI are installed
# system-wide (brew install go, go install github.com/wailsapp/wails/v2/cmd/wails).
PATH_add build/bin
PATH_add "$(go env GOPATH)/bin"

dotenv_if_exists .env
