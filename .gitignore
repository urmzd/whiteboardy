# Build output
build/bin/
bin/

# The embedded frontend bundle is generated by `make build`; the directory
# itself is tracked so `go build` can satisfy //go:embed on a fresh clone.
frontend/dist/*
!frontend/dist/.gitkeep

# Dependencies
node_modules/

# Local state and editor noise
.DS_Store
*.local
.env
